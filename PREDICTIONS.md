The first function will double the numeric value passed to it.

The second function will return the given number multiplied by itself.

The third function will return 'true' if the number inputted is even, and 'false' if it is not.

The fourth function will return 'true' when the number inputted is odd, and 'false' if it is not.